spring.datasource.url=jdbc:mysql://${DB_HOST}:3306/${MYSQL_DATABASE}?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=${MYSQL_USER}
spring.datasource.password=${MYSQL_PASSWORD}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

## Configuraci�n de JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# IMPORTANTE: Se a�ade el dialecto para asegurar que Hibernate sepa qu� versi�n de MySQL usar.
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

## Configuraci�n del Servidor
server.port=8080